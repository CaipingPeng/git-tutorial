URL: https://git-scm.com/docs/git-config
Title: Git - git-config Documentation
================================================================================

[p] git-config - Get and set repository or global options

[pre] git config list[<file-option>] [<display-option>] [--includes]git config get[<file-option>] [<display-option>] [--includes] [--all] [--regexp] [--value=<pattern>] [--fixed-value] [--default=<default>] [--url=<url>] <name>git config set[<file-option>] [--type=<type>] [--all] [--value=<pattern>] [--fixed-value] <name> <value>git config unset[<file-option>] [--all] [--value=<pattern>] [--fixed-value] <name>git config rename-section[<file-option>] <old-name> <new-name>git config remove-section[<file

[h2] DESCRIPTION

[p] You can query/set/replace/unset options with this command. The name is
actually the section and the key separated by a dot, and the value will be
escaped.

[p] Multiple lines can be added to an option by using the--appendoption.
If you want to update or unset an option which can occur on multiple
lines,--value=<pattern>(which is an extended regular expression,
unless the--fixed-valueoption is given) needs to be given.  Only the
existing values that match the pattern are updated or unset.  If
you want to handle the lines that donotmatch the pattern, just
prepend a single exclamation mark in front (see alsoEXAMPLES),
but note that this only works when th

[code] --fixed-value

[code] --fixed-value

[p] The--type=<type>option instructsgit configto ensure that incoming and
outgoing values are canonicalize-able under the given <type>.  If no--type=<type>is given, no canonicalization will be performed. Callers may
unset an existing--typespecifier with--no-type.

[p] When reading, the values are read from the system, global and
repository local configuration files by default, and options--system,--global,--local,--worktreeand--file<filename>can be used to tell the command to read from only
that location (seeFILES).

[p] When writing, the new value is written to the repository local
configuration file by default, and options--system,--global,--worktree,--file<filename>can be used to tell the command to
write to that location (you can say--localbut that is the
default).

[p] This command will fail with non-zero status upon error.  Some exit
codes are:

[li] The section or key is invalid (ret=1),

[p] The section or key is invalid (ret=1),

[li] no section or name was provided (ret=2),

[p] no section or name was provided (ret=2),

[li] the config file is invalid (ret=3),

[p] the config file is invalid (ret=3),

[li] the config file cannot be written (ret=4),

[p] the config file cannot be written (ret=4),

[li] you try to unset an option which does not exist (ret=5),

[p] you try to unset an option which does not exist (ret=5),

[li] you try to unset/set an option for which multiple lines match (ret=5), or

[p] you try to unset/set an option for which multiple lines match (ret=5), or

[li] you try to use an invalid regexp (ret=6).

[p] you try to use an invalid regexp (ret=6).

[p] On success, the command returns the exit code 0.

[p] A list of all available configuration variables can be obtained using thegithelp--configcommand.

[p] List all variables set in config file, along with their values.

[p] Emits the value of the specified key. If key is present multiple times
in the configuration, emits the last value. If--allis specified,
emits all values associated with key. Returns error code 1 if key is
not present.

[p] Set value for one or more config options. By default, this command
refuses to write multi-valued config options. Passing--allwill
replace all multi-valued config options with the new value, whereas--value=will replace all config options whose values match the given
pattern.

[p] Unset value for one or more config options. By default, this command
refuses to unset multi-valued keys. Passing--allwill unset all
multi-valued config options, whereas--valuewill unset all config
options whose values match the given pattern.

[p] Rename the given section to a new name.

[p] Remove the given section from the configuration file.

[p] Opens an editor to modify the specified config file; either--system,--global,--local(default),--worktree, or--file<config-file>.

[p] Default behavior is to replace at most one line. This replaces
all lines matching the key (and optionally--value=<pattern>).

[p] Adds a new line to the option without altering any existing
values. This is the same as providing--value=^$inset.

[p] Append a comment at the end of new or modified lines.

[p] If<message>begins with one or more whitespaces followed
by "", it is used as-is.  If it begins with "", a space is
prepended before it is used.  Otherwise, a string " # " (a
space followed by a hash followed by a space) is prepended
to it.  And the resulting string is placed immediately after
the value defined for the variable.  The<message>must
not contain linefeed characters (no multi-line comments are
permitted).

[p] Withget, return all values for a multi-valued key.

[p] Withget, interpret the name as a regular expression. Regular
expression matching is currently case-sensitive and done against a
canonicalized version of the key in which section and variable names
are lowercased, but subsection names are not.

[p] When given a two-part <name> as <section>.<key>, the value for
<section>.<URL>.<key> whose <URL> part matches the best to the
given URL is returned (if no such key exists, the value for
<section>.<key> is used as a fallback).  When given just the
<section> as name, do so for all the keys in the section and
list them.  Returns error code 1 if no value is found.

[p] For writing options: write to global~/.gitconfigfile
rather than the repository.git/config, write to$XDG_CONFIG_HOME/git/configfile if this file exists and the~/.gitconfigfile doesn’t.

[code] ~/.gitconfig

[code] .git/config

[code] $XDG_CONFIG_HOME/git/config

[code] ~/.gitconfig

[p] For reading options: read only from global~/.gitconfigand from$XDG_CONFIG_HOME/git/configrather than from all available files.

[code] ~/.gitconfig

[code] $XDG_CONFIG_HOME/git/config

[p] See alsoFILES.

